name: test
on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install iverilog
        run: sudo apt-get update && sudo apt-get install -y iverilog

      - name: Setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python packages
        run: pip install -r test/requirements.txt

      - name: Run tests (grep version)
        run: |
          cd test
          make clean
          make

          RESULT_FILE=$(find . -name "results.xml" | head -n 1)

          if [ -z "$RESULT_FILE" ]; then
            echo "❌ No results.xml found. Did cocotb tests run correctly?"
            ls -R .
            exit 1
          fi

          echo "📄 Found results.xml at $RESULT_FILE"
          echo "---- File contents ----"
          cat "$RESULT_FILE"
          echo "-----------------------"

          if grep -q 'failures="[1-9]' "$RESULT_FILE"; then
            echo "❌ Test(s) failed."
            exit 1
          else
            echo "✅ All tests passed."
          fi

      - name: Test Summary
        uses: test-summary/action@v2.3
        with:
          paths: "test/**/*.xml"
        if: always()

      - name: Upload VCD and Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-artifacts
          path: |
            test/**/*.vcd
            test/**/*.xml
